
(define fac (lambda (n)
  (if (islt n 2)
      1
      (mul n (fac (sub n 1))))))

(define main (lambda ()
  (fac 10)))

(define foo-if (lambda (a b c)
  (if (eq a :true)
      b
      c)))

(define some-list-of-symbols
  (list :purple :yellow :red :blue))
